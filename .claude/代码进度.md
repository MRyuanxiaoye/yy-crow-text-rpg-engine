---
last_updated: 2026-05-18
---

## 代码进度总览

> 当前仓库 `main` 分支尚无提交记录，`git log --oneline -5` 与 `git diff HEAD~3` 均不可用。本进度基于 2026-05-18 对当前工作区文件的检查结果。

### 阶段状态

| 阶段 | 状态 | 说明 |
|------|------|------|
| Phase 1 数据层重构 | 已完成 | 新 NPC 数据结构、时间过滤加载、NPC prompt 经历层、旁白公开信息边界均已落地 |
| Phase 2 交互层重构 | 已完成 | GM 解读层、NPC 可达性检查、召回机制、行动面板过滤与位置系统已落地 |
| Phase 3 朝会模式 | 已完成 | 朝会状态、NPC 上下文、飞书卡片、GM 路由与朝会流程均已落地，`python3 -m compileall -q src` 通过 |
| Trace 测试回溯系统 | 已完成 | `GameTracer`、JSONL 存储、prompt 去重、turn 快照、`/trace` 指令与 NPC/旁白/GM 埋点均已落地，`python3 -m compileall -q src` 通过 |

## 已完成

### Phase 1 数据层重构

| 模块 | 路径 | 状态 | 当前证据 |
|------|------|------|------|
| NPC 新格式数据 | `scripts/mingmo/fixed_npcs/` | 完成 | 11 个 NPC YAML 已转换为 `character_seed` + `initial_state` + `timeline` 三段式 |
| 剧本加载器 | `src/scripts/loader.py` | 完成 | 新增 `load_npc_profile_at_time(script_id, npc_name, game_time, state)`；支持按游戏时间过滤 NPC timeline；兼容旧格式 NPC |
| 条件事件判定 | `src/scripts/loader.py` | 完成 | 已修复 `state=None` 时条件事件误触发问题 |
| NPC 对话引擎 | `src/engine/npc_engine.py` | 完成 | 已集成时间过滤后的 NPC profile；新格式 NPC 会注入性格底色、当前状态、关系初值和经历层 |
| NPC 经历层 | `src/engine/npc_engine.py` | 完成 | 已实现“到目前为止发生了什么”的经历层，并插入 NPC system prompt |
| 旁白信息边界 | `src/engine/narrator.py` | 完成 | 已新增叙述者禁令；`_public_npc_summary(state)` 只向旁白暴露 NPC 公开身份摘要 |

### Phase 2 交互层重构

| 模块 | 路径 | 状态 | 当前证据 |
|------|------|------|------|
| 状态管理扩展 | `src/engine/state.py` | 完成 | 新增 `npc_locations` 字段与 `pending_gm_interpretation` 字段，用于记录 NPC 位置和待处理 GM 解读结果 |
| 固定 NPC 初始位置加载 | `src/scripts/loader.py` | 完成 | 新增 `load_all_fixed_npc_initial_locations()`，支持汇总固定 NPC 初始位置 |
| GM 解读层 | `src/engine/game_master.py` | 完成 | 新增 `GM_INTERPRET_PROMPT`、`_gm_interpret()`、`_handle_gm_selection()`，以 GM 解读替代旧意图分类 |
| NPC 交互可达性 | `src/engine/game_master.py` | 完成 | 已实现 NPC 可达性检查；交互前根据区域/位置判断 NPC 是否可被直接接触 |
| NPC 召回机制 | `src/engine/game_master.py` | 完成 | 已实现召回流程与延迟队列处理；不可达 NPC 可进入召回等待 |
| 行动面板过滤 | `src/engine/game_master.py` | 完成 | 行动面板会根据 GM 解读、NPC 可达性与当前场景过滤候选行动 |
| GM 解读卡片 | `src/feishu/card_builder.py` | 完成 | 新增 `build_gm_interpretation_card()`，用于展示和确认 GM 解读结果 |
| 位置系统 | `src/engine/game_master.py` / `src/engine/state.py` / `src/scripts/loader.py` | 完成 | 已实现区域映射、可达性计算、NPC 初始位置接入与召回延迟队列处理 |

### Phase 3 朝会模式

| 模块 | 路径 | 状态 | 当前证据 |
|------|------|------|------|
| 朝会状态字段 | `src/engine/state.py` | 完成 | `GameState` 已新增 `court_session: dict[str, Any]`，并在反序列化流程中恢复 `court_session` |
| NPC 朝会上下文 | `src/engine/npc_engine.py` | 完成 | `generate_npc_reply()` 已支持 `court_context` 参数；prompt 会注入在场人物、议题、正式/私下场合、发言模式和前序发言 |
| 朝会飞书卡片 | `src/feishu/card_builder.py` | 完成 | 已实现 `build_court_session_card()`，展示议题、轮次、NPC 发言、等待召回 NPC，并提供点名、全员发言和结束朝会按钮 |
| 朝会裁判 Prompt | `src/engine/game_master.py` | 完成 | 已新增 `COURT_ARBITER_PROMPT`，用于裁定每轮 NPC 发言人、模式、理由和顺序 |
| 朝会核心流程 | `src/engine/game_master.py` | 完成 | 已实现 `_init_court_session()`、`_handle_court_message()`、`_end_court_session()` 等朝会函数；支持入场、轮次发言、记忆摘要和结束清理 |
| 朝会入口与路由 | `src/engine/game_master.py` | 完成 | `multi_talk` 已替换为朝会初始化入口；消息处理会在 `state.phase == "court"` 且朝会激活时路由到 `_handle_court_message()` |
| 朝会卡片回调 | `src/engine/game_master.py` | 完成 | 已处理 `court_address`、`court_speak_all`、`court_end` 回调，支持点名 NPC、全员发言和结束朝会 |
| 编译检查 | `src/` | 通过 | 2026-05-18 执行 `python3 -m compileall -q src` 通过；本环境无 `python` 命令，使用 `python3` |

### Trace 测试回溯系统 ✅

- `src/engine/tracer.py`: 新建，`GameTracer` 类 + JSONL 存储 + prompt 去重 + turn 快照 ✅
- `src/engine/state.py`: `trace_enabled` 字段 ✅
- `src/engine/game_master.py`: `/trace` 指令 + 10 处埋点 ✅
- `src/engine/npc_engine.py`: NPC 对话 trace ✅
- `src/engine/narrator.py`: 旁白 trace ✅
- 存储路径: `data/traces/{chat_id}/{日期}.jsonl`
- 全部 `py_compile` 通过

### 当前基础引擎模块

| 模块 | 路径 | 状态 | 行数 |
|------|------|------|------|
| 游戏主控 | `src/engine/game_master.py` | 已实现，已接入 Phase 2/3/Trace | 2491 |
| 状态管理 | `src/engine/state.py` | 已实现，已接入 Phase 2/3/Trace | 857 |
| NPC 对话引擎 | `src/engine/npc_engine.py` | 已实现，已接入 Phase 1/3/Trace | 840 |
| 旁白叙事 | `src/engine/narrator.py` | 已实现，已接入 Phase 1/Trace | 688 |
| Trace 回溯系统 | `src/engine/tracer.py` | 已实现 | 122 |
| 剧本加载器 | `src/scripts/loader.py` | 已实现，已接入 Phase 1 | 853 |
| 维度系统 | `src/engine/dimension.py` | 已实现 | 未复核 |
| 章节推进 | `src/engine/progression.py` | 已实现 | 未复核 |
| 时间系统 | `src/engine/time_system.py` | 已实现 | 未复核 |
| 事件调度 | `src/engine/event_scheduler.py` | 已实现 | 未复核 |
| 角色成长 | `src/engine/growth.py` | 已实现 | 未复核 |
| 压力系统 | `src/engine/pressure.py` | 已实现 | 未复核 |
| NPC 生命周期 | `src/engine/npc_lifecycle.py` | 已实现 | 未复核 |
| 记忆系统 | `src/engine/memory.py` | 已实现 | 未复核 |
| 行动系统 | `src/engine/action.py` | 已实现 | 未复核 |
| 种子系统 | `src/engine/seed.py` | 已实现 | 未复核 |
| 结局系统 | `src/engine/ending.py` | 已实现 | 未复核 |
| 判定器 | `src/engine/judge.py` | 已实现 | 未复核 |
| 世界演化 | `src/engine/world_evolution.py` | 已实现 | 未复核 |
| 掷骰系统 | `src/engine/dice.py` | 已实现 | 未复核 |
| 飞书接入 | `src/feishu/` | 已实现，`card_builder.py` 已接入 Phase 2/3 | `card_builder.py` 813 |
| LLM 客户端 | `src/llm/client.py` | 已实现 | 未复核 |

### 明末剧本数据

| 内容 | 状态 |
|------|------|
| 世界层配置 | 已实现 |
| 角色模板 | 已实现 |
| 固定 NPC | 已完成新格式转换，共 11 个 |
| 决策点 | 已实现 |
| 事件池 | 已实现 |
| 多结局 | 已实现 |

## 进行中

当前未发现明确处于编码中的模块。现有工作重点应是：

- 为 Phase 1 补充回归测试或验收脚本。
- 为 Phase 2 补充端到端验收，覆盖 GM 解读、NPC 可达性、召回队列和行动面板过滤。
- 为 Phase 3 补充端到端验收，覆盖 `multi_talk` 朝会入口、点名/全员发言/结束回调、NPC 召回入场和朝会记忆摘要。
- 为 Trace 测试回溯系统补充运行时验收，覆盖 `/trace on/off/clear`、JSONL 写入、prompt 模板去重、turn 快照和 NPC/旁白/GM 埋点。
- 在提交历史建立前，先完成一次基线提交，便于后续使用 `git diff HEAD~3` 审计变更。

## 未开始

当前未发现规划内但尚未开始编码的核心模块。Phase 1、Phase 2、Phase 3 的主体运行时代码均已落地，后续重点转为自动化测试、真实飞书联调和完整游戏流程验收。

## 待确认/未验证

- Phase 1 尚未看到自动化测试结果或端到端验收记录。
- Phase 2 尚未看到自动化测试结果或端到端验收记录。
- Phase 3 已通过 `py_compile`/`compileall` 级别语法检查，但尚未看到自动化测试结果或端到端验收记录。
- Trace 测试回溯系统已通过 `py_compile`/`compileall` 级别语法检查，但尚未看到真实会话下 JSONL 文件写入和回溯读取验收记录。
- Docker 部署运行状态未验证。
- 飞书真实环境联调状态未验证。
- 新格式 NPC 在完整游戏流程中的时间过滤、经历注入、旁白信息边界仍需端到端回归。
- GM 解读、NPC 可达性、召回延迟队列和行动面板过滤仍需完整游戏流程回归。
- 朝会模式的 `multi_talk` 入口、朝会卡片回调、裁判发言顺序、召回 NPC 入场和朝会结束记忆摘要仍需完整游戏流程回归。
- 从小说自动生成剧本的流程尚未实现或验收。
